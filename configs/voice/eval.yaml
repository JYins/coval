schema_version: "1"

# G1 remains blocked until real manifests and provider outputs replace these paths.
reference_manifest: data/voice/manifests/reference.json
prediction_manifest: data/voice/predictions/baseline.json
output: results/voice/baseline_metrics.json
license_inventory: configs/voice/licenses.yaml
asset_manifest: configs/voice/artifacts.local.yaml

metrics:
  der_frame_ms: 10
  der_collar_ms: 250
  score_overlap: true

claim_gate:
  required_corpora:
    - public
    - synthetic
  required_metrics:
    - cer
    - der
    - speaker_attributed_cer
    - structured_f1
    - false_approved_fact_rate
    - rtf
    - peak_ram_mb
